# 0330 PHP修正メモ

## 目的

フロントとAPIをつなぐ前提で、PHP側のどこが破綻していたかを整理する。

## ダメだった点

### 1. `$data` の参照ミスがあった

`create.php` と `update.php` で `data["task"]` のように書かれていた。

これは正しくは `$data["task"]` で、元のままだと JSON を読めず、実行時エラーの原因になる。

## 2. バリデーションの条件が逆だった

いくつかのAPIで、値があるときにエラー、値がないときに成功という逆転した条件になっていた。

### 該当例

- `create.php` の `task` と `deadline`
- `update.php` の `id`
- `delete.php` の `id`

この状態だと、正しいリクエストを送っても失敗する。

## 3. `INSERT` や `UPDATE` の直後に `fetch()` していた

`create.php` と `update.php` で、SQL実行後にそのまま `fetch()` していた。

ただし `INSERT` と `UPDATE` は、そのままでは更新後データを返さない。
そのため、作成後・更新後のデータを返したいなら、もう一度 `SELECT` で取り直す必要がある。

## 4. 成功時のレスポンスが矛盾していた

`create.php` では成功メッセージを返しているのに、`success` が `false` になっていた。

これだとフロント側で「見た目は成功、判定は失敗」という不整合が起きる。

## 5. 変数名の不一致があった

`update.php` では更新後データを `$updatedTask` ではなく別名で返そうとしていた。

このようなズレがあると、レスポンス生成時に未定義変数の問題が起きる。

## 6. 更新対象・削除対象が存在しない場合の扱いが弱かった

元のコードでは、対象の `id` が存在しなくても、わかりやすい失敗レスポンスになっていなかった。

そのため、フロント側から見ると

- 本当に更新・削除できたのか
- 対象が存在しなかっただけなのか

の区別がつきにくかった。

## 7. `me.php` のレスポンス形式が揃っていなかった

未ログイン時は `user: null` を返していたが、ログイン時は `email` だけを返していた。

この状態だとフロント側で

- 未ログイン時は `response.user`
- ログイン時は `response.email`

のように分岐が必要になり、扱いづらい。

ログイン状態に関係なく `user` キーで揃えたほうが安全。

## 8. `list.php` の返却データが将来のフロント連携を考えると不足していた

React側では `complete` や `completed_at` などのキーを使う前提が見えていたが、PHPの一覧APIにはそれがなかった。

今すぐReactを直さないとしても、後で寄せることを考えると、返却JSONのキーをある程度揃えておくほうが移行しやすい。

## 9. CORS の許可メソッドが少なかった

`bootstrap.php` では `GET, POST, OPTIONS` だけが許可されていた。

今後フロントが `PUT` `PATCH` `DELETE` を使うようになると、プリフライトで詰まる可能性がある。

## 今回の修正方針

- 入力値の参照ミスを直す
- 必須チェックを正しい条件にする
- `create` と `update` は実行後に `SELECT` し直して最新データを返す
- `delete` は対象なしを判別できるようにする
- `me.php` のレスポンス形式を揃える
- `list.php` のJSONキーを今後寄せやすい形にする
- CORS を少し広げて将来の詰まりを減らす

## 補足

今回の修正は PHP 側のみで、React側は変更していない。

そのため、次はフロント側を少しずつAPIレスポンスに寄せていく段階になる。
